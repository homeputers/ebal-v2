spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  test:
    database:
      replace: none
server:
  servlet:
    context-path: /api/v1
