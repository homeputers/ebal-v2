{
  "name": "ebal-v2",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace web dev",
    "build": "yarn workspace web build",
    "preview": "yarn workspace web preview",
    "lint": "yarn workspace web lint",
    "typecheck": "yarn workspace web typecheck",
    "i18n:scan": "yarn workspace web i18n:scan",
    "format": "prettier --write . --config packages/config/prettier.cjs",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.7.2",
    "@commitlint/config-conventional": "^17.7.2",
    "husky": "^8.0.3"
  }
}
